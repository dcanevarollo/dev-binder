spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/dev_binder
    username: dcanevarollo
    password: j672fb3i

server:
  error:
    whitelabel:
      enabled: false
    include-stacktrace: never
